{
  "name": "japantravel",
  "version": "1.0.0",
  "description": "",
  "keywords": [
    "Node.js",
    "Express.js",
    "Mustache",
    "Bootstrap",
    "JQuery"
  ],
  "main": "index.js",
  "scripts": {
    "test": "mocha --require esm",
    "build:fabdist": "rm -rf dist/ && mkdir dist/ && cp -r controllers dist/ && cp -r helpers dist/ && cp -r public dist/ && cp -r routes dist/ && cp -r test dist/ && cp -r utils dist/ && cp -r views dist/ && cp index.js dist/ && cp keys.js dist/ && cp package.json dist/",
    "build:fab": "npm run build:fabdist && npm run fab:compile",
    "fab:compile": "fab-static dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fab/static": "^0.1.8",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "mocha": "^7.0.0",
    "mustache": "^4.0.0",
    "mustache-express": "^1.3.0"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.3",
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "supertest": "^4.0.2"
  }
}
